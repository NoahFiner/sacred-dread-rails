# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.

development:
  adapter: postgresql
  encoding: unicode
  # host: localhost # Uncomment if on OS X
  database: sacred_dread_development
  pool: 5

test:
  adapter: postgresql
  encoding: unicode
  # host: localhost # Uncomment if on OS X
  database: sacred_dread_test
  pool: 5

production:
  adapter: postgresql
  encoding: unicode
  # host: localhost # Uncomment if on OS X
  database: sacred_dread_production
  pool: 5